"""
InvenioRDM settings for ultraviolet project.

This file was automatically generated by 'invenio-cli init'.

For the full list of settings and their values, see
https://inveniordm.docs.cern.ch/reference/configuration/.
"""

# requires to load config variables
import os
from datetime import datetime
from datetime import timedelta

from dotenv import load_dotenv
from invenio_rdm_records.config import RDM_FACETS, RDM_SEARCH
from invenio_records_resources.services.records.facets import CFTermsFacet
from invenio_vocabularies.services.custom_fields import VocabularyCF
from invenio_vocabularies.services.facets import VocabularyLabels

from ultraviolet.geoserver.fields import GeoServerCF
from ultraviolet.policies import UltraVioletPermissionPolicy


def _(x):  # needed to avoid start time failure with lazy strings
    return x


# Flask
# =====
# See https://flask.palletsprojects.com/en/1.1.x/config/

# Define the value of the cache control header `max-age` returned by the server when serving
# public files. Files will be cached by the browser for the provided number of seconds.
# See flask documentation for more information:
# https://flask.palletsprojects.com/en/2.1.x/config/#SEND_FILE_MAX_AGE_DEFAULT
SEND_FILE_MAX_AGE_DEFAULT = 300

# load environment variables
load_dotenv()

APP_ENVIRONMENT = os.getenv("APP_ENVIRONMENT", "local")
SECRET_KEY = os.getenv("FLASK_SECRET_KEY")
if not SECRET_KEY:
    raise ValueError("No SECRET_KEY set for Flask application")

PERMANENT_SESSION_LIFETIME = timedelta(days=1)

# Since HAProxy and Nginx route all requests no matter the host header
# provided, the allowed hosts variable is set to localhost. In production it
# should be set to the correct host and it is strongly recommended to only
# route correct hosts to the application.
TRUSTED_HOSTS = os.getenv("TRUSTED_HOSTS", ["0.0.0.0", "localhost", "127.0.0.1"])

# Search hosts
# SEARCH_HOSTS = os.getenv("SEARCH_HOSTS",'localhostxx','port':9200,'timeout':160}])

# Flask-SQLAlchemy
# ================
# See https://flask-sqlalchemy.palletsprojects.com/en/2.x/config/

# TODO: Set
SQLALCHEMY_DATABASE_URI = os.getenv(
    "SQLALCHEMY_DATABASE_URI",
    "postgresql+psycopg2://ultraviolet:ultraviolet@localhost/ultraviolet",
)


# Invenio-App
# ===========
# See https://invenio-app.readthedocs.io/en/latest/configuration.html

APP_DEFAULT_SECURE_HEADERS = {
    "content_security_policy": {
        "default-src": [
            "'self'",
            "data:",  # for fonts
            "'unsafe-inline'",  # for inline scripts and styles
            "blob:",  # for pdf preview
            # Add your own policies here (e.g. analytics)
        ],
        "img-src": [
            "'self'",
            "data:",
            "https://*.basemaps.cartocdn.com",
            "https://maps-public.geo.nyu.edu",
            "https://maps-restricted.geo.nyu.edu",
        ],
    },
    "content_security_policy_report_only": False,
    "content_security_policy_report_uri": None,
    "force_file_save": False,
    "force_https": True,
    "force_https_permanent": False,
    "frame_options": "sameorigin",
    "frame_options_allow_from": None,
    "session_cookie_http_only": True,
    "session_cookie_secure": True,
    "strict_transport_security": True,
    "strict_transport_security_include_subdomains": True,
    "strict_transport_security_max_age": 31556926,  # One year in seconds
    "strict_transport_security_preload": False,
}

USERS_RESOURCES_GROUPS_ENABLED = True

# Flask-Babel
# ===========
# See https://python-babel.github.io/flask-babel/#configuration

# Default locale (language)
# See https://invenio-i18n.readthedocs.io/en/latest/configuration.html
BABEL_DEFAULT_LOCALE = "en"
# Default time zone
BABEL_DEFAULT_TIMEZONE = "UTC"


# Invenio-I18N
# ============
PREVIEWER_CHARDET_BYTES = 8192
"""Number of bytes to read for character encoding detection by `cchardet`."""

FILES_REST_TASK_WAIT_MAX_SECONDS = 6000
FILES_REST_MULTIPART_CHUNKSIZE_MAX = 2 * 1024 * 1024 * 1024

# Disables requirement for user email confirmation
SECURITY_CONFIRMABLE = False

# The following is a hacky implementation. It should be False for a fresh invenio installation. After creating a community and adding the nyuusers role to the community, add the community ID as a list here
# For example: "auto_update_communities": ["c441b9c2-40e0-4035-96cc-8e13bd28d0f7"] where c441b9c2-40e0-4035-96cc-8e13bd28d0f7 is the community ID retrieved from the communities_metadata table. If one
# community is updated here, no need to update for subsequent communities, they add new group members automatically.
COMMUNITIES_AUTO_UPDATE = False

# Custom Ultraviolet Permission Policy
RDM_PERMISSION_POLICY = UltraVioletPermissionPolicy
# Invenio-Theme
# =============
# See https://invenio-theme.readthedocs.io/en/latest/configuration.html

# Name used in header and UI
THEME_SITENAME = "UltraViolet"
# Frontpage title
THEME_FRONTPAGE_TITLE = "Search UltraViolet"
# Header logo
THEME_LOGO = "images/logo_ultraviolet_1word_white.svg"

# ===============
# See https://invenio-app-rdm.readthedocs.io/en/latest/configuration.html

# Instance's theme entrypoint file. Path relative to the ``assets/`` folder.
INSTANCE_THEME_FILE = "./less/theme.less"


# Invenio-Records-Resources
# =========================
# See https://github.com/inveniosoftware/invenio-records-resources/blob/master/invenio_records_resources/config.py

SITE_UI_URL = os.getenv("SITE_UI_URL", "https://127.0.0.1:5000")

SITE_API_URL = os.getenv("SITE_API_URL", "https://127.0.0.1:5000/api")

APP_RDM_DEPOSIT_FORM_DEFAULTS = {
    "publication_date": lambda: datetime.now().strftime("%Y-%m-%d"),
    "rights": [
        {
            "id": "cc-by-4.0",
            "title": "Creative Commons Attribution 4.0 International",
            "description": (
                "The Creative Commons Attribution license allows "
                "re-distribution and re-use of a licensed work "
                "on the condition that the creator is "
                "appropriately credited."
            ),
            "link": "https://creativecommons.org/licenses/by/4.0/legalcode",
        }
    ],
    "publisher": "NYU University",
}

# See https://github.com/inveniosoftware/invenio-app-rdm/blob/master/invenio_app_rdm/config.py
APP_RDM_DEPOSIT_FORM_AUTOCOMPLETE_NAMES = "search"  # "search_only" or "off"

# Invenio-RDM-Records
# ===================
# See https://inveniordm.docs.cern.ch/customize/dois/
# DOI disabled and not configured
datacite_enabled = os.getenv("DATACITE_ENABLED", "false")

if datacite_enabled.lower() == "true":
    datacite_enabled = True
else:
    datacite_enabled = False

DATACITE_ENABLED = datacite_enabled

DATACITE_USERNAME = os.getenv("DATACITE_USERNAME", "")
DATACITE_PASSWORD = os.getenv("DATACITE_PASSWORD", "")
DATACITE_PREFIX = os.getenv("DATACITE_PREFIX", "")
DATACITE_TEST_MODE = os.getenv("DATACITE_TEST_MODE", True)
DATACITE_DATACENTER_SYMBOL = os.getenv("DATACITE_DATACENTER_SYMBOL", "")

PROPAGATE_EXCEPTIONS = True
PIDSTORE_DATACITE_URL = "https://api.test.datacite.org"

APP_RDM_DEPOSIT_FORM_QUOTA = {
    "maxFiles": 100,
    "maxStorage": 10**9 * 50,
}

# Authentication - Invenio-Accounts and Invenio-OAuthclient
# =========================================================
# See: https://inveniordm.docs.cern.ch/customize/authentication/


# Allow download all
RDM_ARCHIVE_DOWNLOAD_ENABLED = True

# Allow access to old admin panel
ADMIN_ROLE = "admin"
ADMIN_PERMISSION_FACTORY = "ultraviolet.policies.ultraviolet_admin_permission_factory"


# Invenio-Accounts
# ----------------
# See https://github.com/inveniosoftware/invenio-accounts/blob/master/invenio_accounts/config.py
# Local login settings

# Default to True in Dev but override to False in Prod
ACCOUNTS_LOCAL_LOGIN_ENABLED = os.getenv("ACCOUNTS_LOCAL_LOGIN_ENABLED", True)

# Default to True in Dev but override to False in Prod
SECURITY_RECOVERABLE = os.getenv(
    "SECURITY_RECOVERABLE", True
)  # local login: allow users to reset the password

# Default to True in Dev but override to False in Prod
SECURITY_CHANGEABLE = os.getenv(
    "SECURITY_CHANGEABLE", True
)  # local login: allow users to change password

SECURITY_REGISTERABLE = False  # local login: allow users to register
SECURITY_CONFIRMABLE = False  # require users to confirm their e-mail address

# Microsoft Entra ID / OpenID Connect
# ===================================
# See: https://invenio-oauthclient.readthedocs.io/en/latest/configuration.html
# and Microsoft Entra OIDC endpoints:
# https://learn.microsoft.com/en-us/entra/identity-platform/v2-protocols-oidc [[2]](https://learn.microsoft.com/en-us/entra/identity-platform/v2-protocols-oidc)
# Invenio oauthclient remote app configuration pattern:
# https://invenio-oauthclient.readthedocs.io/en/latest/configuration.html [[1]](https://invenio-oauthclient.readthedocs.io/en/latest/configuration.html)

ENTRA_TENANT_ID = os.getenv("ENTRA_TENANT_ID", None)
ENTRA_CLIENT_ID = os.getenv("ENTRA_CLIENT_ID", None)
ENTRA_CLIENT_SECRET = os.getenv("ENTRA_CLIENT_SECRET", None)

ENTRA_BASE_URL = f"https://login.microsoftonline.com/{ENTRA_TENANT_ID}/oauth2/v2.0"
ENTRA_APP_CREDENTIALS = {
    "consumer_key": ENTRA_CLIENT_ID,
    "consumer_secret": ENTRA_CLIENT_SECRET,
}

# Only enable OAuth if ENTRA_TENANT_ID is set
if ENTRA_TENANT_ID is not None:
    OAUTHCLIENT_REMOTE_APPS = dict(
        entra=dict(
            title="Microsoft Entra ID",
            description="Sign in with Microsoft Entra ID",
            icon="fa-brands fa-microsoft",
            authorized_handler="ultraviolet.entraid.oauth:entra_authorized_handler",
            disconnect_handler="invenio_oauthclient.handlers:disconnect_handler",
            params=dict(
                request_token_params=dict(scope="openid profile email User.Read"),
                base_url=ENTRA_BASE_URL,
                request_token_url=None,
                access_token_url=f"{ENTRA_BASE_URL}/token",
                authorize_url=f"{ENTRA_BASE_URL}/authorize",
                app_key="ENTRA_APP_CREDENTIALS",
                access_token_method="POST",
            ),
            client_id=ENTRA_CLIENT_ID,
            client_secret=ENTRA_CLIENT_SECRET,
        )
    )

from invenio_oauthclient.views.client import auto_redirect_login

ACCOUNTS_LOGIN_VIEW_FUNCTION = auto_redirect_login
OAUTHCLIENT_AUTO_REDIRECT_TO_EXTERNAL_LOGIN = False

# Disable local login if you want Entra to be the only login path
ACCOUNTS_LOCAL_LOGIN_ENABLED = True

# Optional: keep account creation automatic for first-time Entra users
SECURITY_REGISTERABLE = False
SECURITY_CONFIRMABLE = False

# Inregrity files report
APP_RDM_ADMIN_EMAIL_RECIPIENT = "uv-staff@nyu.edu"
MAIL_DEFAULT_SENDER = "uv@nyu.edu"
FILES_INTEGRITY_REPORT_SUBJECT = "File integrity report"
MAIL_SUPPRESS_SEND = False

# Invenio-UserProfiles
# --------------------
USERPROFILES_READ_ONLY = True  # Logins are managed by SSO and shouldn't be editable

# OAI-PMH
# =======
# See https://github.com/inveniosoftware/invenio-oaiserver/blob/master/invenio_oaiserver/config.py

OAISERVER_ID_PREFIX = "ultraviolet.library.nyu.edu"
"""The prefix that will be applied to the generated OAI-PMH ids."""

# Invenio-Search
# --------------

SEARCH_INDEX_PREFIX = "ultraviolet-"

# Invenio-Users-Resources
# -----------------------

USERS_RESOURCES_ADMINISTRATION_ENABLED = True

# build webpack no symlinks (only needed in production)
if APP_ENVIRONMENT == "global":
    COLLECT_STORAGE = "flask_collect.storage.file"
    COLLECT_STATIC_ROOT = os.getenv("COLLECT_STATIC_ROOT", "/opt/static")

if APP_ENVIRONMENT == "local":
    RDM_RECORDS_USER_FIXTURE_PASSWORDS = {"adminUV@test.com": "adminUV"}

# custom pages
PAGES_DEFAULT_TEMPLATE = "invenio_app_rdm/default_static_page.html"
"""Default template to render."""

PAGES_TEMPLATES = [
    ("invenio_app_rdm/default_static_page.html", "Default"),
]

APP_RDM_PAGES = {
    "documentation-api": "/documentation-api",
}

RATELIMIT_ENABLED = False

# UltraViolet custom vars
# =======================

UV_VERSION = open(".uv-version", "r").read().strip()
UV_DEPOSIT_REQUEST = "https://nyu.qualtrics.com/jfe/form/SV_exhm5HuMKRdW0hU"
UV_FAQS = "https://guides.nyu.edu/ultraviolet/faq"

RDM_HOMEPAGE = "https://inveniosoftware.org/products/rdm"
DATA_SERVICES_HOMEPAGE = "https://guides.nyu.edu/dataservices"

# About UltraViolet
UV_DEPOSIT_ASSISTANCE = "https://library.answers.nyu.edu/form?queue_id=6256"
UV_SERVICE_POLICIES = "https://guides.nyu.edu/ultraviolet/policies"
UV_DOCS = "https://guides.nyu.edu/ultraviolet/"
UV_TECH_DOCS = "https://nyudlts.github.io/ultraviolet/"

# Partnerships
DLTS_HOMEPAGE = "https://wp.nyu.edu/library-dlts/"
DS_HOMEPAGE = "https://guides.nyu.edu/dataservices"
DCN_HOMEPAGE = "https://datacurationnetwork.org/"
RDM_DOCS_HOMEPAGE = "http://inveniordm.docs.cern.ch"

# Resources
UV_DEPOSIT_GUIDE = "https://guides.nyu.edu/ultraviolet/deposit"
SCIP_HOMEPAGE = (
    "https://library.nyu.edu/departments/scholarly-communications-information-policy/"
)
NYU_LIBRARIES_HOMEPAGE = "https://library.nyu.edu"

# Max file size for displaying download buttons and link (50 GB)
MAX_FILE_SIZE = 50 * 1024 * 1024 * 1024

CELERY_BROKER_URL = "redis://localhost:6379/0"

# Enable FAIR Signposting
APP_RDM_RECORD_LANDING_PAGE_FAIR_SIGNPOSTING_LEVEL_1_ENABLED = True

# Enable Audit Logs
AUDIT_LOGS_ENABLED = True

GEOSERVER_PUBLIC_URL = "https://maps-public.geo.nyu.edu/geoserver/sdr"
GEOSERVER_RESTRICTED_URL = "https://maps-restricted.geo.nyu.edu/geoserver/sdr"

RDM_NAMESPACES = {
    "geospatial": "https://example.com/",
    "geoserver": "https://geoserver.org/",
}

RDM_CUSTOM_FIELDS = [
    VocabularyCF(
        name="geospatial:resource_type",
        vocabulary_id="geospatialresourcetypes",
        dump_options=True,
        multiple=False,
    ),
    GeoServerCF(
        name="geoserver",
        public_server=GEOSERVER_PUBLIC_URL,
        restricted_server=GEOSERVER_RESTRICTED_URL,
    ),
]

RDM_CUSTOM_FIELDS_UI = [
    {
        "section": "Geospatial",
        "fields": [
            {
                "field": "geospatial:resource_type",
                "ui_widget": "Dropdown",
                "template": "/geospatial_resource_type.html",
                "props": {
                    "label": "Resource Type",
                    "placeholder": "lidar",
                    "description": "The type of geospatial data",
                    "search": False,
                    "multiple": False,
                    "clearable": True,
                },
            }
        ],
    },
    {
        "section": "GeoServer",
        "fields": [
            {
                "field": "geoserver",
                "ui_widget": "GeoServerFields",
                "template": "/geoserver.html",
                "props": {
                    "publicServerUrl": GEOSERVER_PUBLIC_URL,
                    "restrictedServerUrl": GEOSERVER_RESTRICTED_URL,
                    "label": _("GeoServer"),
                    "layer": {
                        "label": _("Layer Name"),
                        "placeholder": _("sdr:nyu_2451_12345"),
                        "description": _("The name of the layer in GeoServer"),
                    },
                    "has_wms": {
                        "label": "Has WMS layer?",
                        "description": "Is this layer available via WMS?",
                    },
                    "has_wfs": {
                        "label": "Has WFS layer?",
                        "description": "Is this layer available via WFS?",
                    },
                    "bounds": {
                        "label": _("Layer Bounds"),
                        "placeholder": _(
                            "ENVELOPE(-178.2176, -66.969275, 71.406235818, 18.921781818)"
                        ),
                        "description": _(
                            "The ENVELOPE that defines the boundaries of this layer"
                        ),
                    },
                },
            }
        ],
    },
]

RDM_FACETS = {
    **RDM_FACETS,
    "geospatial": {
        "facet": CFTermsFacet(
            field="geospatial:resource_type.id",
            label=_("Geospatial resource types"),
            value_labels=VocabularyLabels("geospatialresourcetypes"),
        ),
        "ui": {
            "field": CFTermsFacet.field("geospatial:resource_type"),
        },
    },
}

RDM_SEARCH = {**RDM_SEARCH, "facets": RDM_SEARCH["facets"] + ["geospatial"]}

# Enable the browsing of Subcommunities in Communities
COMMUNITIES_SHOW_BROWSE_MENU_ENTRY = True

# Enable Web Archive Viewer and Support
FILES_REST_ALLOW_RANGE_REQUESTS = True
PREVIEWER_WEB_ARCHIVE_RANGE_REQUESTS = True

PREVIEWER_PREVIEWERS_ORDER = [
    "invenio_previewer.extensions.web_archive",
    "invenio_previewer.extensions.csv_papaparsejs",
    "invenio_previewer.extensions.json_prismjs",
    "invenio_previewer.extensions.xml_prismjs",
    "invenio_previewer.extensions.pdfjs",
    "invenio_previewer.extensions.iiif_simple",
    "invenio_previewer.extensions.simple_image",
    "invenio_previewer.extensions.mistune",
    "invenio_previewer.extensions.zip",
    "invenio_previewer.extensions.ipynb",
    "invenio_previewer.extensions.audio_videojs",
    "invenio_previewer.extensions.video_videojs",
    "invenio_previewer.extensions.txt_previewer",
    "invenio_previewer.extensions.default",
]

PREVIEWER_PREFERENCE = [
    "web_archive",
    "csv_papaparsejs",
    "json_prismjs",
    "xml_prismjs",
    "pdfjs",
    "iiif_simple",
    "simple_image",
    "mistune",
    "zip",
    "ipynb",
    "audio_videojs",
    "video_videojs",
    "txt",
]
"""Decides which previewers are available and their priority."""
